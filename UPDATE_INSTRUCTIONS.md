# 🔄 Инструкции по обновлению бота

## ❌ Проблема
Бот показывает: "Вы достигли дневного лимита генераций (5)"

## ✅ Решение

У вас есть **2 варианта**:

---

### 🎯 Вариант 1: Обновить код (РЕКОМЕНДУЕТСЯ)

На вашем Mac выполните:

```bash
cd /path/to/testbot  # Путь к вашему проекту
git pull origin claude/fix-telegram-request-error-014ERoLMhaYFSebCxF17eJu5
```

Это загрузит все исправления:
- ✅ Убраны лимиты генерации (999999 вместо 5)
- ✅ Увеличены timeouts (300s вместо 60s)
- ✅ Улучшено качество видео (1280x720 HD)
- ✅ Добавлен retry с exponential backoff
- ✅ Исправлен lip sync

После pull:
```bash
# Остановите бота (Ctrl+C если запущен)
# Запустите снова
python -m bot.main
```

---

### 🔧 Вариант 2: Временный сброс лимитов (если нельзя обновить код)

Если вы не можете обновить код прямо сейчас:

```bash
# На сервере где находится репозиторий
cd /home/user/testbot
python3 reset_limits.py
```

Это удалит failed задачи и сбросит счётчики.

⚠️ **ВНИМАНИЕ:** Это временное решение! Лимит в 5 вернётся через день.

---

## 📋 Проверка после обновления

После git pull проверьте:

```bash
grep "MAX_DAILY_REQUESTS" bot/config.py
```

Должно быть:
```python
MAX_DAILY_REQUESTS = int(os.getenv('MAX_DAILY_REQUESTS', '999999'))  # Unlimited
```

---

## 🆘 Если проблема осталась

Удалите базу данных и начните заново:

```bash
rm bot.db  # Удалит БД со всеми лимитами
python -m bot.main  # БД создастся автоматически
```

---

## 📞 Поддержка

Если проблема не решается, покажите вывод:

```bash
python -c "from bot.config import Config; print(Config.MAX_DAILY_REQUESTS)"
```
