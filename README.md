# Telegram Bot для генерации видео через HeyGen API

Telegram-бот для автоматической генерации видео с AI-аватарами и синтезированными голосами через HeyGen API.

## 📋 Описание

Этот бот позволяет пользователям создавать видео с виртуальными аватарами, которые озвучивают заданный текст. Бот взаимодействует с HeyGen API для генерации профессиональных видео с AI-персонажами.

### Основные возможности

- 🎬 Генерация видео с AI-аватарами
- 🎤 Выбор различных голосов для озвучки
- 📝 Поддержка текста до 2000 символов
- ⏱️ Асинхронная обработка запросов
- 🔒 Система лимитов (5 видео в день)
- 📊 Отслеживание статуса генерации
- 💾 Хранение истории запросов

## 🚀 Быстрый старт

### Требования

- Python 3.9 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- HeyGen API Key (получить на [HeyGen](https://www.heygen.com/))

### Установка

1. **Клонируйте репозиторий:**

```bash
git clone <repository-url>
cd testbot
```

2. **Создайте виртуальное окружение:**

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости:**

```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**

```bash
cp .env.example .env
```

Отредактируйте `.env` и заполните необходимые данные:

```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
HEYGEN_API_KEY=your_heygen_api_key_here
```

5. **Запустите бота:**

```bash
python -m bot.main
```

## 📖 Использование

### Команды бота

- `/start` - Начало работы с ботом
- `/help` - Подробная инструкция
- `/generate` - Создание нового видео
- `/status` - Проверка статуса текущей генерации
- `/cancel` - Отмена текущей задачи

### Способы создания видео

#### Способ 1: Пошаговый

1. Отправьте команду `/generate`
2. Введите ID аватара (например: `avatar_123abc`)
3. Введите ID голоса (например: `voice_456def`)
4. Введите текст для озвучки

#### Способ 2: Одним сообщением

Отправьте все данные в формате:
```
avatar_id | voice_id | Ваш текст для озвучки
```

Пример:
```
avatar_123abc | voice_456def | Привет! Это демонстрационное видео.
```

### Получение ID аватаров и голосов

Для использования бота вам нужны ID аватаров и голосов из HeyGen:

1. Войдите в свой аккаунт HeyGen
2. Перейдите в раздел с аватарами/голосами
3. Скопируйте нужные ID

> **Примечание:** ID представляют собой буквенно-цифровые строки, например: `avatar_abc123` или `voice_xyz789`

## 🏗️ Структура проекта

```
testbot/
├── bot/
│   ├── __init__.py
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── handlers/
│   │   ├── __init__.py
│   │   ├── commands.py      # Обработчики команд
│   │   └── conversation.py  # Обработчик диалогов
│   ├── services/
│   │   ├── __init__.py
│   │   ├── heygen_api.py    # Интеграция с HeyGen API
│   │   └── video_service.py # Логика генерации видео
│   ├── database/
│   │   ├── __init__.py
│   │   ├── models.py        # Модели БД
│   │   └── database.py      # Работа с БД
│   └── utils/
│       ├── __init__.py
│       └── validators.py    # Валидация входных данных
├── requirements.txt
├── .env.example
├── .gitignore
└── README.md
```

## ⚙️ Конфигурация

Все настройки хранятся в `.env` файле:

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | (обязательно) |
| `HEYGEN_API_KEY` | API ключ HeyGen | (обязательно) |
| `DATABASE_URL` | URL подключения к БД | `sqlite+aiosqlite:///bot.db` |
| `MAX_DAILY_REQUESTS` | Лимит запросов в день | `5` |
| `VIDEO_CHECK_INTERVAL` | Интервал проверки статуса (сек) | `15` |
| `VIDEO_GENERATION_TIMEOUT` | Таймаут генерации (сек) | `600` |
| `MAX_TEXT_LENGTH` | Макс. длина текста | `2000` |
| `VIDEO_WIDTH` | Ширина видео | `1920` |
| `VIDEO_HEIGHT` | Высота видео | `1080` |
| `TEMP_VIDEO_DIR` | Папка для временных файлов | `./temp_videos` |

## 💾 База данных

Бот использует SQLite по умолчанию. Для production рекомендуется PostgreSQL.

### Миграция на PostgreSQL

1. Установите дополнительные зависимости:

```bash
pip install asyncpg
```

2. Измените `DATABASE_URL` в `.env`:

```env
DATABASE_URL=postgresql+asyncpg://user:password@localhost/dbname
```

### Структура БД

**Таблица `users`:**
- `user_id` - Telegram ID пользователя
- `username` - Имя пользователя
- `registration_date` - Дата регистрации
- `total_videos_generated` - Всего созданных видео
- `last_request_date` - Последний запрос

**Таблица `video_tasks`:**
- `task_id` - ID задачи
- `user_id` - ID пользователя
- `heygen_video_id` - ID видео в HeyGen
- `avatar_id` - ID аватара
- `voice_id` - ID голоса
- `input_text` - Текст для озвучки
- `status` - Статус (pending/processing/completed/failed)
- `created_at` - Дата создания
- `completed_at` - Дата завершения
- `video_url` - URL готового видео
- `error_message` - Сообщение об ошибке

## 🔧 Разработка

### Логирование

Логи записываются в:
- Консоль (stdout)
- Файл `bot.log`

Уровень логирования: `INFO`

### Обработка ошибок

Бот включает обработку следующих ошибок:
- Невалидные ID аватаров/голосов
- Превышение лимита запросов
- Ошибки HeyGen API
- Таймауты генерации
- Ошибки сети

## 🚀 Развертывание

### Docker (рекомендуется)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.9-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "-m", "bot.main"]
```

Запустите:

```bash
docker build -t telegram-heygen-bot .
docker run -d --env-file .env telegram-heygen-bot
```

### systemd (Linux)

Создайте `/etc/systemd/system/heygen-bot.service`:

```ini
[Unit]
Description=HeyGen Telegram Bot
After=network.target

[Service]
Type=simple
User=your-user
WorkingDirectory=/path/to/testbot
Environment="PATH=/path/to/testbot/venv/bin"
ExecStart=/path/to/testbot/venv/bin/python -m bot.main
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:

```bash
sudo systemctl daemon-reload
sudo systemctl enable heygen-bot
sudo systemctl start heygen-bot
```

## 📝 Лицензия

MIT License

## 👥 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

## 🔄 Обновления

### v1.0.0
- Начальная версия
- Базовая функциональность генерации видео
- Поддержка двух способов ввода данных
- Система лимитов
- Логирование и обработка ошибок
